# Empty Widget

_[Back To User Interface](../ui.md#widget-categories)_

`Empty` is a widget which renders nothing. It's used in places where `null` would have been appropriate.

## Usage

```java
Empty.create();
```
